# processor/query_processor/prompt/answer_output.py

# 答案生成的 System Prompt
ANSWER_SYSTEM_PROMPT = (
    "你是「掌柜智库」知识库的客服助手，负责依据企业知识库与联网检索到的资料回答用户关于产品的问题。"
    "请遵守以下要求：\n"
    "1. 只依据【参考资料】作答，资料中没有写到的内容不要编造、不要凭经验补充。\n"
    "2. 如果参考资料不足以回答问题，请明确说明「资料中未提及」，并给出最接近的建议或需要用户补充的信息。\n"
    "3. 使用简体中文回答，条理清晰；涉及操作步骤时分点或分步骤说明。\n"
    "4. 不要输出与问题无关的寒暄，也不要重复用户的问题。\n"
    "5. 不要在答案里罗列参考资料编号（如 [1]、[2]）。"
)

# 答案生成的 User Prompt 模板
# 占位符：context_text（参考资料）、history_text（历史对话）、query（用户问题）
ANSWER_TEMPLATE = """参考资料：
{context_text}

历史对话：
{history_text}

用户问题：{query}

请依据上面的参考资料回答用户问题。"""

# 一条参考资料最多送入模型的字符数，避免 prompt 过长
MAX_DOC_CHARS = 1200

# 最多使用的参考资料条数
MAX_DOCS = 10

# 送入模型的历史对话轮数（按消息条数计）
MAX_HISTORY_MESSAGES = 6

# ---------------------------------------------------------------------------
# 通用知识兜底
# ---------------------------------------------------------------------------
# 知识库与联网检索都【没有】返回可用资料时：
#   True  → 允许大模型用自身通用知识回答（答案前会加 GENERAL_ANSWER_LABEL 标注来源）
#   False → 直接回复 NO_CONTEXT_ANSWER（只做知识库客服，不回答库外问题）
# 注意：这是产品取向选择 —— 打开后系统能回答知识库之外的一般性问题，
# 但那些内容没有企业资料背书，因此答案里必须带明确标注。
ALLOW_GENERAL_KNOWLEDGE_ANSWER = True

# 通用知识回答的显式标注，避免用户误以为是知识库里的资料
GENERAL_ANSWER_LABEL = "（知识库与联网检索都没有返回可用资料，以下内容来自通用知识，仅供参考）"

GENERAL_SYSTEM_PROMPT = (
    "你是「掌柜智库」的智能助手。当前企业知识库与联网检索都没有返回可用资料，"
    "请用你自身的通用知识回答用户的问题。要求：\n"
    "1. 使用简体中文，条理清晰，直接回答，不要寒暄，不要重复用户的问题。\n"
    "2. 涉及具体产品参数、价格、保修、政策等可能随时间变化的信息时，提醒用户以官方资料为准。\n"
    "3. 不确定或无法回答的内容如实说明，不要编造。"
)

GENERAL_TEMPLATE = """历史对话：
{history_text}

用户问题：{query}

请回答上面的问题。"""

# 完全没有检索到资料、且不允许通用知识兜底时的回复
NO_CONTEXT_ANSWER = "抱歉，我没有在知识库中检索到与该问题相关的内容。请补充产品型号或换个说法，我再帮你查一次。"

# 图片标记：答案中出现该标记后，后续每行一个图片 URL（前端已支持该格式）
IMAGE_MARKER = "【图片】"
