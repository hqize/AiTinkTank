shamefully-hoist=true
electron_mirror=https://npmmirror.com/mirrors/electron/
# electron-builder 打包时要从 GitHub 拉 winCodeSign / nsis 等辅助工具，
# 国内网络连不上就靠这个键走镜像。键名会被 npm/pnpm 转成环境变量
# npm_config_electron_builder_binaries_mirror，正是 electron-builder 优先读取的名字。
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
